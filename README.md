# APC_C
A command-line calculator implemented in C that performs arithmetic on integers of unlimited size. The program supports addition, subtraction, multiplication, and division by representing numbers using doubly linked lists, enabling computations beyond standard integer limits.
